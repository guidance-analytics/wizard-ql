{
  "name": "wizard-ql",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "type": "module",
  "types": "dist/index.d.ts",
  "files": ["dist/"],
  "scripts": {
    "build": "rm -r dist && bun build --target=node --format=esm --packages external --minify src/index.ts --outfile dist/index.js && tsc --project tsconfig.build.json"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "eslint": "^9.25.1",
    "eslint-plugin-jsdoc": "^50.6.11",
    "neostandard": "^0.12.1"
  },
  "optionalDependencies": {
    "knex": "^3.1.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
